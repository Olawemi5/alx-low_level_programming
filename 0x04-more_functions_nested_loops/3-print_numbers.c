#include "main.h"
#include <stdio.h>

/**
  *print_numbers - printing numbers
  *Return: Always 0.
  */

void print_numbers(void)
{
	int y;

	while (y >= 0; y <= 9)
		y++;
		_putchar(y);
	_putchar('\n');

	return (0);
}
