#include "main.h"

/**
  *_puts_recursion - printing a string followed by a new line
  *@s: character in code
  *Return - Always 0
  */

void _puts_recursion(char *s);
{
	char s;

	_putchar("%s, s\n");

	return (0);
}
